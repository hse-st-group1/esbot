# Instructions for Test Execution

1. Navigate to the backend folder:
    - e.g.
    ```bash
    cd esbot/application/backend/
    ```
2. Run the tests using Maven
    - Run tests using Maven locally:
        ```bash
        ./mvnw test
        ```
    - Run tests with Maven installed:
        ```bash
        mvn test
        ```